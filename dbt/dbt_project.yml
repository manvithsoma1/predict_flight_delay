name: flight_delay
version: "1.0.0"
config-version: 2

profile: flight_delay

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  flight_delay:
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    mart:
      +materialized: table